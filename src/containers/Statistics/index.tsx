import React from 'react';

type Props = {};

export default function Statistics({}: Props) {
  return (
    <div>
      {
        //SVG Draw Dotted Circle and Ticker Count Stat
      }
    </div>
  );
}
